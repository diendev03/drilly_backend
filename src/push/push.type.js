/**
 * Push Type Enum
 * Extensible notification types - add new types without modifying core logic
 */

const PUSH_TYPE = {
    CHAT: "chat",
    ORDER: "order",
    SYSTEM: "system",
    FOLLOW: "follow",
    CALL: "call",
};

module.exports = { PUSH_TYPE };
